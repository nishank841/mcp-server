apiVersion: v1
kind: Service
metadata:
  name: mcp-server-svc
  namespace: mcp-server
spec:
  selector:
    app: mcp-server
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30082
  type: NodePort
